# Frontend workspace

This folder contains the React, TypeScript, Tailwind, SVG, and optional React Three Fiber learning interface. Run the full application from the repository root with `pnpm dev`; the backend serves the frontend and the tRPC API together during local development.

The 3D result scene is loaded only when a learner opens it. The accessible SVG result stage and plain-English explanation remain available on devices without WebGL.
