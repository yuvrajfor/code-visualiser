# Reference Inspection Notes

The provided reference URL renders a dark educational visualization app titled “Chai Visual — Learn CS by watching it move”. At the captured viewport, the browser shows a 178px left navigation rail and a main workspace. The rail contains “DSA Visual”, a “Search…” field with a keyboard hint, an expanded “Two Pointers” group, the active “Two Sum II” row in an off-white rectangular tile, and the next “Arrays & Hashi…” group. The main workspace shows a small “PROBLEM” eyebrow, the large title “Two Sum II”, LeetCode and difficulty pills, a “details” pill, a “Hide solutions” control, and a tab-like “Brute force” header.

The intro card reads “Sorted input · find indices that add up to target” and includes the explanation of the 1-indexed sorted array. Below it are “ASKED AT” company tags. The visualization heading row has “Brute force” and “Optimized · two pointers”, then six outlined array cells with values 2, 5, 8, 11, 15, 19 and index labels [0] through [5]. On the right, the Brute force code card has a peach “Practice” button, a “copy” control, syntax-colored lines, and a horizontal scroll strip; below it is a state card with target 19.

The bottom of the viewport contains a bordered narration card with a small line badge and the sentence “We want two positions (i, j) such that arr[i] + arr[j] = 19. We scan with 0-based positions and add 1 at the end, because the answer is 1-indexed.” A compact playback bar beneath includes restart, previous, play, next, an orange progress marker on a thin track, and a “1×” speed pill.

